[project]
name = "hospital-manager"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "django>=5.2.7",
    "django-stubs[mypy-compatible]>=5.2.7",
]
